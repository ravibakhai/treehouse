<%= form_for([@property, @property.reservations.new]) do |f| %>

	<div class="row">
		<div class="col-md-6">
			<label>Check In</label>
			<%= f.text_field :start_date, readonly: 'true', placeholder: 'Start Date' %>
		</div>
		<div class="col-md-6">
			<label>Check Out</label>
			<%= f.text_field :end_date, readonly: 'true', placeholder: 'End Date', disabled: 'true' %>
		</div>
	</div>

	<%= f.hidden_field :property_id, value: @property.id %>
	<%= f.hidden_field :price, value: @property.price %>
	<%= f.hidden_field :total, id: 'reservation_total' %>


	<div id="preview" style="display: none">
		<h4><span id="message"></span></h4>

		<table class="reservation-table" >
			<tbody>
				<tr>
					<td>Day(s)</td>
					<td><span id="reservation_days"></span></td>
				</tr>
				<tr>
					<td>Total</td>
					<td><span id="reservation_sum"></span></td>
				</tr>
			</tbody>
		</table>
	</div>

	<br>
	<%= f.submit "Book Now", class: "btn btn-primary" %>

<% end %>

<script>
	$(function() {

		$('#reservation_start_date').datepicker({
			dateFormat: 'dd-mm-yy'
		});
		$('#reservation_end_date').datepicker({
			dateFormat: 'dd-mm-yy'
		});

	});
</script>
